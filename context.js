import { createContext } from 'react'

// default app context values
export const ThemeContext = createContext('light')
export const LanguageContext = createContext('ru')
export const LocationContext = createContext('Minsk')